import { Token } from '../scanner/token';
import { Expression } from './expression';

export class Parser {
	// private readonly tokens: Token[];
	// private current = 0;

	constructor() {
		// tokens: Token[]
		// this.tokens = tokens;
	}

	public parse(): object {
		return {};
	}
}
